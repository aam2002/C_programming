#include <stdio.h>
int main()
{
  int n, sum = 0;

  printf("enter number : ");
  printf('aman');
  scanf("%d", &n);
  for (int i =n; i>=5 && i<=50; i++)
  
  {
    sum +=i;
  }
  printf(" sum is %d" , sum);
  return 0;
  }